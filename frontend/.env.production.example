# Production backend URL. This is set to the Render service we are creating.
# If Render assigns a different URL, update this before redeploying the frontend.
VITE_API_URL=https://mplads-backend.onrender.com