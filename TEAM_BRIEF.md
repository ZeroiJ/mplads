# TEAM BRIEF (plain English, no jargon)

Auto-generated by `scripts/team_brief.py` at **2026-08-30 13:42 UTC**.

## What we are doing

We teach a language model (118M params, free open-source) to recognise when two work descriptions in the MPLADS data are the **same work re-submitted as a new one** ('Bhavan' vs 'Bhawan', 'STANCH POST LIGHT' vs 'STAND POST LIGHTS'). This is the 'duplicate claim' part of our fraud detector.

Training data: **70,607 real pairs** from our master file - 49,424 lessons (train), 10,591 practice exams (val), 10,592 final exams (test). Each lesson teaches it one duplicate + one non-duplicate. Model: `paraphrase-multilingual-MiniLM-L12-v2`

## Where things stand right now

| Lesson # | Does it spot duplicates? (val %) | Finds the duplicate in top-20 (test %) | Train loss (lower = better) | Elapsed |
|---|---|---|---|---|
| 1.0 | 0.9565 | 0.45 | 0.0547 | 510.4s |

### Plain-English verdict after lesson 1.0:

- It **correctly rejects pure look-a-likes 95.7%** of the time (higher = better).
- When it hunts through 10,592 works for the true duplicate, the right answer is in its **top-20 picks 45.0%** of the time.
- Baseline (before any training) to compare later: script `evaluate_after.py`.
- `1.0` lessons done → model has seen the full dataset that many times. More lessons = more sharpening.

## Frozen baseline (BEFORE training - captured, will not change)

We scored the untrained model on 10 fixed 'expert' pairs so we can prove what training changed later.

- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)
- **?**: similarity ? (higher = more confident same work)

## Honest scope (we told ourselves this)

- Training sharpens **same-language, near-identical** duplicate detection ('Bhavan' vs 'Bhawan').
- It does **NOT** fix cross-language matching ('Shauchalaya' vs 'toilet') - the frozen model already handles that; the data simply has no Hindi-English bridge pairs. That is a **data gap, not a training failure**, and we will say so in the demo.
- The demo-critical fraud path (rules + anomaly detection + evidence dossier + legal note) uses the frozen model and needs **zero training**. This fine-tune is the bonus slide.

## Artifacts (where things live)
- Models: `models/best/epoch_<N>/` + `models/best/best.txt` (best pick)
- Results table: `metrics/metrics.csv`  |  Live watch: `scripts/live_view.py`
- Before/after comparison: `python scripts/evaluate_after.py`
- Raw training pairs: `data/pairs.csv`
